apiVersion: apps/v1
kind: Deployment
metadata:
  name: build-master
  namespace: mini-lambda
spec:
  replicas: 1
  selector:
    matchLabels:
      app: build-master
  template:
    metadata:
      labels:
        app: build-master
    spec:
      enableServiceLinks: false
      serviceAccountName: build-master
      containers:
        - name: build-master
          image: ghcr.io/${GITHUB_USER}/build-master:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: mini-lambda-config
          env:
            - name: SERVICE_NAME
              value: "build-master"
---
apiVersion: v1
kind: Service
metadata:
  name: build-master
  namespace: mini-lambda
spec:
  ports:
    - port: 8082
      targetPort: 8082
  selector:
    app: build-master
