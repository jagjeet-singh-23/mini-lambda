apiVersion: v1
kind: Secret
metadata:
  name: mini-lambda-secrets
  namespace: mini-lambda
type: Opaque
stringData:
  # In a production cluster, use an external secret operator or sealed secrets.
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "postgres"
  RABBITMQ_USER: "guest"
  RABBITMQ_PASS: "guest"
  MINIO_ROOT_USER: "minioadmin"
  MINIO_ROOT_PASSWORD: "minioadmin"
  GRAFANA_USER: "admin"
  GRAFANA_PASS: "admin"
